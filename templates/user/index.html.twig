{% extends 'base.html.twig' %}

{% block title %}{{ user.username }}{% endblock %}

{% block body %}
<div class="container">
    <h3>{{ user.username }}</h3>
    <h3>{{ user.email }}</h3>
</div>
{% endblock %}
